---
'@medalsocial/meda': minor
---

Add `@medalsocial/meda/calendar` surface with `<Calendar>`, `<MonthView>`, `<WeekView>`, `<DayView>`, and `<CalendarToolbar>`. Fully prop-driven (events array + handlers), container-query responsive, dark-mode aware, and i18n via per-component `Labels` props.
