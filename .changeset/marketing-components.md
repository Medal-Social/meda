---
'@medalsocial/meda': minor
---

Add the `@medalsocial/meda/marketing` subpath with framework-agnostic marketing components: `MarketingCallout`, `MarketingContact`, `MarketingLeadMagnet`, and shared CTA rendering. Storybook, demo, and registry entries now expose a Marketing category.
