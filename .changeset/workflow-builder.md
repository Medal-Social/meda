---
"@medalsocial/meda": minor
---

Add `@medalsocial/meda/workflow-builder` surface — a prop-driven React Flow canvas with default node types (trigger, action, condition, delay, end), a tappable toolbox panel, presentational header, and list-view cards. Adds `@xyflow/react` as a required peer dependency.
